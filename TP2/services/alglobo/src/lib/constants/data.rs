use std::time::Duration;

// ----------------------------------------------------------------------------

pub const WAIT_ALL_RESPONSES_TIMEOUT: Duration = Duration::from_secs(10);
pub const N_PREPARE_RETRIES: u32 = 3;
